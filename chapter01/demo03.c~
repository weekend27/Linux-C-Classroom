
/*
**读取输入字符,当不是文件结束符时,输出该字符,然后继续读下一个字符.
*/

#include<stdio.h>

void main(){

	int c;
	printf("请输入任意字符串:\n");
	c = getchar();
	printf("\n");
	printf("刚才输入的字符串是:\n");
	while(c != EOF){
		//printf("刚才输入的字符串是:\n");
		putchar(c);
		c = getchar();
	}
}
